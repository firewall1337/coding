#include <iostream>
#include <bits/stdc++.h>
#include <string>
using namespace std;

int main()
{
    int l,p;
    cin>>l>>p;
    if (l>=13 && (p >= 800) && (p <=1500) )
    {
        cout<<"Prediction is right";
    }
    else{
        cout<<"Go to thana";
    }
    return 0;
}