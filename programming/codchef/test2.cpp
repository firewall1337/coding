#include <iostream>
#include <bits/stdc++.h>
#include <string>
using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int r1,r2,w1;
        int w2,c1,c2;
        cin>>r1>>w1>>c1;
        cin>>r2>>w2>>c2;
        if ( (r1 > r1 && w1 > w2) || (x1 > y1) && x3 > y3 )
        {
            cout<<"A"<<endl;
        }
        else{
            cout<<"B"<<endl;
        }
    }
    return 0;
}